name = "car-usage-tracker"
compatibility_date = "2026-05-09"
pages_build_output_dir = "public"

[[d1_databases]]
binding = "DB"
database_name = "car-tracker-db"
database_id = "35471c6e-8141-4bec-a791-5e971f3a61d1"

[[r2_buckets]]
binding = "PHOTOS"
bucket_name = "car-tracker-photos"

[vars]
ADMIN_USER_ID = "U3e8201d1672346cac5fde4a9bc8d728c"
ALLOWED_ORIGIN = "https://fn-car.pages.dev"

# ── Selfbot Integration ──

# คำสั่งรันเพื่อหาลิ้งค์ cloudflared tunnel --url http://localhost:5000
# กรอก URL ที่ได้มาจาก cloudflared tunnel เช่น http://****.trycloudflare.com/webhook (ย้ำหากใส่ลิ้งค์ใหม่ต้องมีคำว่า /webhook ต่อท้าย ถ้าไม่มี เมื่อกดบันทึกการใช้งานรถหรือคืนรถ flex จะไม่แจ้งเตือนเข้าไลน์กลุ่ม)
# และ secret key สำหรับยืนยันว่า request มาจากเว็บจริงๆ
SELFBOT_WEBHOOK_URL = "https://appreciate-clock-luxury-similar.trycloudflare.com/webhook"
SELFBOT_SECRET = "MyCarBot2026!"
